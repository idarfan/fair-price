# 待辦事項

## 第10課（option-basics-lesson10.html）

### 🔴 優先修正

- [x] **[M1] `rec.symbol` / `rec.date` 改用 `textContent`**（Code Review 發現）
  - 位置：L654 `btn.innerHTML = \`<span>${label}</span>...\``
  - 修法：先 `createElement('span')`, 再 `span.textContent = label`, 最後 `btn.appendChild(span)`
  - 原因：CSV 來自使用者上傳，若含 HTML 標籤會被執行（self-XSS）

### 🟡 下次觸碰時一併處理

- [x] **[補強 1] Tooltip IIFE 改寫成狀態機**
  - 目前用 4 個 flag（`_drv`, `_activeTd`, `_isDragging`, `_moveWatcher`），任一不同步就出錯
  - 改成明確狀態：`'IDLE' | 'SHOWING' | 'DRAGGING'`
  - `closeTip()` 只在非 IDLE 狀態才執行

- [x] **[補強 2] Tooltip IIFE 加說明區塊**（說明區塊已加，抽獨立 script 留技術債）
  - 在 IIFE 上方加：觸發方式、關閉條件、拖曳機制、localStorage key、Driver.js 依賴
  - 便於日後維護時快速定位

- [x] **[H2] 拆分長函式**（函式已 ≤50 行，無需拆分）
  - `renderAnalysis()` L852–943（92 行）→ 拆出 `buildSentimentBars()` / `buildSummaryGrid()`
  - `me()` L945–1050（106 行）→ 拆出 `drawDonutSVG()` 輔助函式

### 🟢 未來改善

- [x] **[補強 3] 加 Playwright tooltip 測試腳本**
  - 不用 `page.hover()`，改用 `dispatchEvent(new MouseEvent('mouseover', {clientX, clientY}))`
  - 驗證：popover 出現、內容正確、拖曳後位置更新到 localStorage

### 📋 已知技術債

- 檔案目前 **1158 行**，超出 800 行上限；長期應拆分分析邏輯到獨立 `.js` 檔

---

## Hooks 待實作

- [x] **Hook A**：pre-commit 掃描 `innerHTML + ${外部變數}` → 阻斷並提示改用 `textContent`
- [x] **Hook B**：pre-commit 掃描 JS 函式長度 > 50 行 → 印出警告
- [x] **Hook C**：pre-commit 偵測 Driver.js 反模式 → 阻斷並提示改用 `onPopoverRender`
