# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Session Start

- **Always review `tasks/lessons.md` at session start** for relevant project patterns and past corrections.

## Work Habits

### Plan Mode Default
- Enter plan mode for ANY non-trivial task (3+ steps or architectural decisions)
- If something goes sideways, STOP and re-plan immediately — don't keep pushing
- Use plan mode for verification steps, not just building
- Write detailed specs upfront to reduce ambiguity

### Subagent Strategy
- Use subagents liberally to keep main context window clean
- Offload research, exploration, and parallel analysis to subagents
- For complex problems, throw more compute at it via subagents
- One task per subagent for focused execution

### Self-Improvement Loop
- After ANY correction from the user: update `tasks/lessons.md` with the pattern
- Write rules for yourself that prevent the same mistake
- Ruthlessly iterate on these lessons until mistake rate drops

### Verification Before Done
- Never mark a task complete without proving it works
- Diff behavior between main and your changes when relevant
- Ask yourself: "Would a staff engineer approve this?"
- Run tests, check logs, demonstrate correctness

### Demand Elegance (Balanced)
- For non-trivial changes: pause and ask "is there a more elegant way?"
- If a fix feels hacky: "Knowing everything I know now, implement the elegant solution"
- Skip this for simple, obvious fixes — don't over-engineer

### Autonomous Bug Fixing
- When given a bug report: just fix it. Don't ask for hand-holding
- Point at logs, errors, failing tests — then resolve them
- Zero context switching required from the user

## Task Management

1. **Plan First**: Write plan to `tasks/todo.md` with checkable items
2. **Verify Plan**: Check in before starting implementation
3. **Track Progress**: Mark items complete as you go
4. **Explain Changes**: High-level summary at each step
5. **Document Results**: Add review section to `tasks/todo.md`
6. **Capture Lessons**: Update `tasks/lessons.md` after corrections

## Core Principles

- **Simplicity First**: Make every change as simple as possible. Impact minimal code.
- **No Laziness**: Find root causes. No temporary fixes. Senior developer standards.

---

## Ruby LSP 使用規則（強制）

本專案已安裝 `ruby-lsp` plugin，處理 Ruby 程式碼時**必須優先使用 LSP 工具**，禁止用 grep/正規表達式取代語意分析。

### 優先順序

| 需求 | 優先使用 | 禁止替代方式 |
|------|----------|-------------|
| 查找方法定義 | LSP Go to Definition | `grep def method_name` |
| 查找所有參考 | LSP Find References | `grep method_name` 全域搜尋 |
| 查看類別成員 | LSP Document Symbols | 手動掃描檔案 |
| 重新命名符號 | LSP Rename Symbol | 手動 find-and-replace |
| 型別/參數提示 | LSP Hover | 逐行閱讀原始碼推斷 |
| 語法診斷 | LSP Diagnostics | 等執行期錯誤才發現 |

### 操作原則

- **讀懂再改**：修改任何 Ruby 方法前，先用 LSP 確認所有呼叫端（Find References），避免破壞其他地方
- **重新命名用 LSP**：跨檔案的符號更名必須走 LSP Rename，不用手動 sed/replace_all
- **診斷優先**：拿到錯誤訊息前，先跑 LSP Diagnostics 確認語法無誤
- **Grep 僅用於文字搜尋**：只有在搜尋字串字面值、註解、或 LSP 不支援的非 Ruby 檔案時，才退回使用 Grep

---

## 文件規範

建立任何新 app 時，**必須在專案根目錄建立 `docs/` 目錄**，並撰寫以下三份主要文件：

| 檔案 | 內容 |
|------|------|
| `docs/INSTALL.md` | 系統需求、相依套件安裝、環境變數設定、資料庫初始化、啟動步驟、常見問題 |
| `docs/USER_MANUAL.md` | 功能概覽、各工具操作說明、API 使用範例、快速鍵 |
| `docs/ARCHITECTURE.md` | 設計原則、技術棧、目錄結構、資料流程圖、元件說明、安全性設計、新增功能步驟 |

文件需與程式碼同步維護：每次新增重要功能後，同步更新受影響的文件章節。

---

## Commands

```bash
# Start / manage the server (port 3003)
systemctl --user restart fairprice
systemctl --user status  fairprice
journalctl --user -u fairprice -n 30

# Lint (auto-fix correctable offenses)
bundle exec rubocop
bundle exec rubocop -a

# Boot check
bundle exec rails runner "puts 'Boot OK'"

# Routes
bundle exec rails routes

# Lookbook component previews (dev only)
open http://localhost:3003/lookbook
```

## Architecture

**No database. No React. No Vite.** This is a pure Phlex + Tailwind CDN app backed by Finnhub HTTP calls.

The app hosts two tools under one process on port 3003:

| Tool | Route | Controller | Namespace |
|------|-------|------------|-----------|
| FairPrice | `/`, `/valuations/:ticker` | `ValuationsController` | `FairValue::` |
| Daily Momentum | `/momentum` | `ReportsController` | `DailyMomentum::` |
| JSON API | `/api/v1/valuations/:ticker` | `Api::V1::ValuationsController` | — |

Both tools share:
- `ApplicationComponent` base class (`app/components/application_component.rb`) — formatting helpers (`fmt_currency`, `fmt_percent`, `fmt_large`, `change_color`, `upside_color`)
- `ApplicationHelper` — momentum risk helpers (`risk_level`, `max_position_note`)
- `FairValue::AppSwitcherComponent` — left sidebar rendered in the main layout
- `app/views/layouts/application.html.erb` — shared layout with navbar + sidebar

## FairPrice data flow

```
ValuationsController#show
  └── StockDataService.fetch(ticker)       → Finnhub /quote, /metric, /profile2, /recommendation
        └── ValuationService.calculate(data, discount_rate)
              → classifies stock type → applies DCF/P-E/PEG/DDM/P-B/EV-EBITDA
```

Stock type classification in `ValuationService` drives which valuation methods are used. To add a new method: write a private method returning `{ method:, value:, note:, formula: }` and add it to the relevant `*_methods` array — the Phlex components render it automatically.

## Daily Momentum data flow

```
ReportsController#index
  └── MomentumReportService#call
        ├── FinnhubService#quote("^VIX")   → vix
        ├── FinnhubService#quote(symbol)×N → stocks  (symbols from config/watchlist.yml)
        ├── FinnhubService#market_news     → news
        └── FinnhubService#earnings_calendar → earnings
```

Edit `config/watchlist.yml` to change tracked symbols — no code change needed.

Market time segment (`:market_hours`, `:pre_market`, `:after_hours`, `:closed`) is derived from ET clock time in `MomentumReportService#time_segment`.

## Markdown 渲染規則（嚴格遵守）

### 必用設定
- gem：`kramdown` + `kramdown-parser-gfm`
- 呼叫：`Kramdown::Document.new(text, input: "GFM").to_html`

### AI 表格的根本解法：由 Ruby 預建，不讓 AI 生成

**凡是資料已在伺服器端計算好的表格，一律由 Ruby 建立 markdown 表格，放進 prompt，告訴 AI 原文輸出。**

```ruby
def build_xxx_table(...)
  lines = ["| 指標 | 數值 |", "|---|---|"]
  rows.each { |name, val| lines << "| #{name} | #{val} |" }
  lines.join("\n")
end
```

在 prompt 中加入：
```
## 數據表格（請在對應小節原文輸出，不得更改任何符號或格式）
#{build_xxx_table(...)}
```

> 前車之鑑：動量觀察表格長期壞掉，根本原因是讓 AI 自己排表格。改由 `OuouAnalysisService#build_momentum_table` 在 Ruby 端產生後放進 prompt，問題徹底解決。

### normalize_md_tables（備用保障用，仍需保留）

AI 生成的其他表格（如支撐阻力位）仍可能使用非 ASCII 破折號或空行，`normalize_md_tables` 作為最後防線。

**`separator_row?` 正確實作**（用 Unicode 屬性，不用字元白名單）：
```ruby
def separator_row?(line)
  s = line.strip
  s.start_with?("|") && s.end_with?("|") && s.length > 2 &&
    !s.match?(/[\p{L}\p{N}]/)  # 沒有任何字母或數字 = separator row
end
```

**兩步驟 normalize**：
- Pass 1：所有 `separator_row?` → 換成 `|---|...|`（欄數 = `line.count("|") - 1`）
- Pass 2：移除緊接在 separator row 前的空行

**務必在修改渲染邏輯後重啟 server 並清除 cache**
```bash
systemctl --user restart fairprice
bundle exec rails runner "Rails.cache.clear"
```
production/systemctl 環境不自動熱重載。**執行前須告知使用者並取得同意。**

## 第三方函式庫選用規則

引入任何 CDN / gem / npm 函式庫前，**必須先確認**：
1. **有無持續維護**：近期是否仍有更新，是否已停止維護
2. **是否支援專案所用的軟體規格**：Tailwind v4 使用 `oklch()` 色彩函數，選用 CSS 相關函式庫時須特別確認

> 前車之鑑：`html2canvas@1.4.1` 是最新版但已停止維護，不支援 `oklch()`，導致 PNG 匯出失效。已改用持續維護的 `html-to-image`。

## 語言規範

- 所有說明、註解、文件、與使用者的溝通，一律使用**繁體中文**
- **嚴格禁用簡體中文慣用術語**，常見違禁詞：兜底→備用保障、踩坑→踩到問題、復盤→事後檢討、賦能→強化、閉環→完整流程、抓手→切入點、痛點→問題點、落地→落實、對齊→確認一致、賽道→領域、底層邏輯→核心邏輯

## Phlex 2.x rules (critical)

- `raw` requires `.html_safe`: `raw <<~JS.html_safe`
- `format()` conflicts with a Phlex internal → use `sprintf()` instead

## Adding a new tool to the sidebar

1. Add a route in `config/routes.rb`
2. Create controller + view under the new namespace
3. Add the tool entry to `APP_LINKS` in `app/components/fair_value/app_switcher_component.rb`
